[project]
name = "whattodo"
version = "0.1.0"
description = "Что мне делать? :-) Текст в концепции «Общие Цели»"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    # text-forge plugin from PyPI
    "sg-text-forge>=0.1.0",
]
